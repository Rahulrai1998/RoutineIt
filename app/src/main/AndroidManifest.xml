<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahul.routineit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/routine_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/routine_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PasswordOtp"></activity>
        <activity android:name=".ForgotPassword" />
        <activity android:name=".SignupTeacher" />
        <activity android:name=".SignupModules" />
        <activity android:name=".TeacherLogin" />
        <activity android:name=".LoginModules" />
        <activity
            android:name=".WelcomePage"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"> <!-- This will hide the action bar or status bar -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>